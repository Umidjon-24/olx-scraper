# Copy this to .env for local testing (never commit .env to git)
# Set these same variables in Railway → your service → Variables tab

# Supabase connection pooler credentials
DB_USER=postgres.kaowfkjtwxeywtikpopw
DB_PASS=Abdurakhmon
